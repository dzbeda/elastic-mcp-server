{
  "mcpServers": {
    "elasticsearch": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "--network", "mcpnet",
        "-e", "ES_URL=http://elasticsearch:9200",
        "docker.elastic.co/mcp/elasticsearch:latest",
        "stdio"
      ]
    }
  }
}
